#ifndef OBJETO_HPP
#define OBJETO_HPP

#include <bits/stdc++.h>

class Classe {
public:
    std::string nome;
    std::vector<std::string> atributos_string;
    std::vector<std::string> atributos_lista;
};

#endif
